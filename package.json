{
    "scripts": {
        "install": "cd backend && npm install && cd .. && cd frontend && npm install",
        "back": "cd ./backend && node --watch app.js",
        "front": "cd ./frontend && npm run dev"
    }

}
